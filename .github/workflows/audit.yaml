name: Audit

on:
  push:
    branches:
      - main
  workflow_dispatch:
  pull_request:
    types: [ opened, synchronize, reopened ]


jobs:
  audit:
    name: npm Audit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3

      - name: Set up NodeJS
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: npm Audit
        run: npm i --package-lock-only && npm audit --json > audit-reports/npm.json
        continue-on-error: true

      - name: Bandit check
        uses: jpetrucciani/bandit-check@main
        with:
          path: '.'
          bandit_flags: '-o audit-reports/bandit.json -f json'
        continue-on-error: true

      - name: Upload Audit Reports
        uses: actions/upload-artifact@v3
        with:
          name: audit-reports
          path: audit-reports/
        if: always()
